/**
 * Face Health Digital Twin — Frontend Configuration
 *
 * Set BACKEND_WS to your Render deployment URL before deploying to Vercel.
 * Format: wss://YOUR-SERVICE-NAME.onrender.com/ws
 *
 * For local development:
 *   BACKEND_WS = "ws://localhost:8000/ws"
 */
window.BACKEND_WS = "wss://YOUR-SERVICE-NAME.onrender.com/ws";

// Uncomment for local development:
// window.BACKEND_WS = "ws://localhost:8000/ws";
